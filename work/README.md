# work/
このディレクトリには、最近の実験ごとの作業ディレクトリを保存しています。

## 役割
最近の開発・実験作業は、root直下の古いスクリプトではなく、`work/` 配下の実験単位ディレクトリで管理する方針です。

## 方針
GitHub移行前整理では、`work/` は最近の作業ディレクトリとしてroot直下に残しました。

`.gitignore` でも `work/` はディレクトリごと除外しない方針です。
