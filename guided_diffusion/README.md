# guided_diffusion/
このディレクトリには、diffusion model本体の実装コードを保存しています。

## 役割
`scDiffusion` の中心となるdiffusion model関連コードを置く本体コードディレクトリです。

## 整理時の扱い
GitHub移行前整理では、`guided_diffusion/` は本体コードとして扱い、root直下に残しました。

このディレクトリは `LegacyFiles/` には移動していません。
